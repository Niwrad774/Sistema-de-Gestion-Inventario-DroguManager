package com.example.inventario.backend.model;

// Enum con los roles disponibles en el sistema.
// Cada usuario tiene exactamente uno de estos roles.
public enum RolUsuario {
    COMPRADOR,
    GESTOR_PEDIDOS,
    ADMIN_INVENTARIO
}
